# Similar to K8s' PersistentVolumeClaim
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: spread1ctl2reps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3G
